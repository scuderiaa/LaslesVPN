import logo from '../../assets/Logo.svg'

export const NavBar = () => {
    return (
        <div>
            <div className="flex items-center">
                <img src={logo} alt="logo" className="" />
            </div>
        </div>
    )
}
